import React from 'react';

// import Header from './Navbar';

import {mount, shallow} from 'enzyme';

describe('<Navbar/>', () => {

  xit('Mounts successfully', () => {
    // const mountedNavbar = shallow(<Header/>); 
  });
  
  // xit ('Has the right number of elements', () => {
  //   const mountedNavbar = mount(<Header/>);
  //   expect(mountedNavbar.find('').children().length).toBe(4);
  // });
});
// Import every components from the parent folders
// Export them so they can be easily accessible through the applications

import Video from "./video";
import Content from "./StudenHome.js";

module.exports = {
  Content,
  Video
};
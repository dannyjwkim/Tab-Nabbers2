/**
 * Export Sign in and Sign up so it can easily be imported else where
 * and also applies styles on the sign up component
 */
import Signin from './Signin';
import Signup from './Signup';

module.exports = {
  Signin,
  Signup
};
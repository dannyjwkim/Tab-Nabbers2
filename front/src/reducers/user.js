const initial_state = {

};

const user = (state = initial_state, action) => {

    switch (action.type) {
        case "value":
            // TODO 
            break;
    
        default:
            return state;
    }
}

export default user;